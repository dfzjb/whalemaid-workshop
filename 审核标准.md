# 审核标准（维护者核对清单）

收录新皮肤前逐项核对，全部通过才合并：

| # | 检查项 | 规则 |
|---|---|---|
| 1 | 包校验 | `validate:pack` 全规则通过（pack-validator 四组：zip 安全 / 路径穿越 / 扩展名白名单 / 结构素材元数据） |
| 2 | id 唯一 | 与现有索引 + 内置角色（xiaojing）不冲突 |
| 3 | license 合法 | 枚举内（CC0 / CC-BY-4.0 / CC-BY-NC-4.0 / MIT / CUSTOM / RIGHTS_RESERVED）；CUSTOM/RIGHTS_RESERVED 必须附 NOTICE.txt 且仅个人自用不列入公共索引 |
| 4 | 素材安全 | 无 NSFW / 侵权素材（人工看缩略图 + 抽帧） |
| 5 | 元数据 | summary ≤60 字；version 为 semver；downloadUrl 为 http(s) 且可达；sha256 与 zip 实际一致 |
| 6 | 缩略图 | ≤100KB（Action 自动压缩，作者提交原图即可） |

## 下架流程

DMCA / 投诉 / 违反标准 → 维护者删除索引条目与 zip，重建 index.json（分钟级）。客户端已安装的包不受影响（本地自治）。

## 供应链安全

- GitHub Action 最小权限（`contents: write` 仅限 main 分支）
- CODEOWNERS 双人复核（可选启用）
- 分支保护：禁止直接 push main，PR 必须审核
- 索引只存哈希不存内容：即使索引仓库被盗，客户端下载仍强制 sha256 + M4 安装校验，最坏结果是装进"合法但难看的皮肤"，无代码执行面
